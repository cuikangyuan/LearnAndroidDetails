<resources>

    <!-- Base application theme.
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="custom_style">@style/custom_style_in_theme</item>s
        <item name="custom_color1">@color/green</item>


    </style>
    -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--
        <item name="android:windowContentTransitions">true</item>
        -->
    </style>

    <!-- 2 主题中配置的style -->
    <style name="custom_style_in_theme">
        <item name="custom_color1">@color/blue</item>
    </style>


    <!--直接在layout中引用的style -->
    <style name="custom_style_in_layout">
        <item name="custom_color1">@color/aqua</item>
    </style>

    <!--定义默认style -->
    <style name="default_style">
        <item name="custom_color1">@color/red</item>
        <!--
        <item name="custom_color2">@color/green</item>
        <item name="custom_color3">@color/blue</item>
        -->
    </style>

    <style name="myToggle" parent="@android:style/Widget.CompoundButton">
        <item name="android:button">@drawable/toggle_show_password</item>
    </style>

    <style name="pop_window_anim_style">
        <item name="android:windowEnterAnimation">@anim/pop_show_anim</item>
        <item name="android:windowExitAnimation">@anim/pop_dismiss_anim</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@anim/pop_push_up_in</item>
        <item name="android:windowExitAnimation">@anim/pop_push_down_out</item>
    </style>

    <style name="popWindow_anim_style" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_up_in</item>
        <item name="android:windowExitAnimation">@anim/pop_down_out</item>
    </style>
</resources>
